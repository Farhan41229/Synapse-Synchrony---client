import ChatContainer from '../../components/chat/ChatContainer';

/**
 * ChatPage Component
 * Protected route for the main chat interface
 */
const ChatPage = () => {
  return (
    <div className="h-screen overflow-hidden">
      <ChatContainer />
    </div>
  );
};

export default ChatPage;
